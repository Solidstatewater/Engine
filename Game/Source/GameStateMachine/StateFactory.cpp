#include "Game_pch.h"
#include "StateFactory.h"

using namespace Anubis;

State * StateFactory::VCreateState(AINT32 i32Id) const
{
	switch(i32Id)
	{
	};

	//the state couldn't be created
	return NULL;
}